from setuptools import setup, find_packages

setup(
    name='dpTools2',
    version='2.1.0',
    description='analysis tools for molecular dynamics simulations (primarily the deformable particle model).',
    author='marshall',
    packages=find_packages(),
)
